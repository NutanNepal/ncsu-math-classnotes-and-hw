\question{
Let $\{P_\lambda\}_{\lambda\in \Lambda}$ be a non-empty family of prime ideals,
and suppose that the $P_\lambda$ are totally ordered by inclusion; then
$\bigcap{P_\lambda}$ is a prime ideal. Also, if $I$ is any proper ideal,
the set of prime ideals containing $I$ has a minimal element.

}